# Server-side Redis connection for durable onboarding drafts.
# Optional locally (localStorage fallback still works).
# Required in production for cross-session / cross-device draft persistence.
# Never commit a real value — set this in Vercel / your host's env config.
REDIS_URL=
