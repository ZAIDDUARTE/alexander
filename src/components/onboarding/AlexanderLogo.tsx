export function AlexanderLogo({ className = "" }: { className?: string }) {
  return (
    <span
      className={`font-serif text-2xl font-semibold tracking-tight text-[var(--color-alexander-navy)] ${className}`}
    >
      Alexander
    </span>
  );
}
